/*
    Array.hpp

    This file holds the interface file for Array.hpp
*/

#ifndef ARRAY_HPP
#define ARRAY_HPP

template<typename T>
class Array {
    T* arr = nullptr;
    int length = 0;
};

#endif
